{
  "mcpServers": {
    "agentqa": {
      "command": "npx",
      "args": ["-y", "agentqa", "mcp", "--dir", "."],
      "env": {
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
      }
    }
  }
}
